<?php

echo("page works");
    // $title = $_POST['title'];
    // $seo = $_POST['seo'];
    // $content = $_POST['content'];
    // $category = $_POST['category']; // example 1 2


    // $db = mysql_connect("localhost7882", "root", "", "themesale");

    // $query = "INSERT INTO post(title, seo_title, content, author) VALUES ($title, $seo, $content, $category)";
    // mysql_exec($db, $query);
?>